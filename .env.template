# ===============================================
# Image-Post Project Environment Variables
# ===============================================
#
# 이 파일을 `.env`로 복사하여 사용하거나,
# CI/CD 또는 배포 환경에 이 변수들을 설정하십시오.

# --- Spring Profile ---
# 'local' (기본값): 로컬 Docker/파일 스토리지 사용
# 'dev': AWS S3 및 외부 DB 사용
SPRING_PROFILES_ACTIVE=local


# --- Database Settings ---
# 'local' 프로필 기본값 (Docker Compose와 일치)
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/imagepost_db
SPRING_DATASOURCE_USERNAME=postgres
SPRING_DATASOURCE_PASSWORD=postgres
# 'dev' 프로필에서는 실제 배포된 DB(예: AWS RDS) 주소로 이 변수들을 덮어써야 합니다.


# --- Local Storage (for 'local' profile only) ---
# (local 기본값: ./uploads/)
FILE_UPLOAD_DIR=./uploads/


# --- AWS S3 & Credentials (for 'dev' profile only) ---
SPRING_CLOUD_AWS_CREDENTIALS_ACCESS_KEY=YOUR_AWS_ACCESS_KEY
SPRING_CLOUD_AWS_CREDENTIALS_SECRET_KEY=YOUR_AWS_SECRET_KEY
SPRING_CLOUD_AWS_REGION_STATIC=ap-northeast-2
CLOUD_AWS_S3_BUCKET=YOUR_S3_BUCKET_NAME